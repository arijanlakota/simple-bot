module.exports = {
	name: 'cat',
	aliases: false,
	description: false,
	cooldown: 5,
	guildOnly: true,
	args:true,
	usage:false,
	// eslint-disable-next-line no-unused-vars
	execute(msg, args) {
        async function forCats(){
            const res = await fetch()
        }
    }
};